/**
 * Site-specific JavaScript.
 *
 * Dev Kit Italia components are already registered by elements.js.
 * This file is for site-specific behavior only.
 *
 * If you need i18n (translations for component UI strings),
 * import registerTranslation from @italia/i18n and register
 * your locale. For a static Jekyll site this is optional —
 * components default to Italian when <html lang="it"> is set.
 */

// Example: nothing needed for the skeleton.
// Components self-register when elements.js loads.
